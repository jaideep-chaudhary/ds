let V = 49;
let arr = [1, 2, 5, 10, 20, 50, 100, 500, 1000]
let n = arr.length
let result = []

for(let i = n-1; i >= 0; i--) {
    while(V >= arr[i]) {
        V -= arr[i]
        result.push(arr[i])
    }
    
}
console.log(result)
// Time complexity: O(N)
// Space complexity: O(1)
